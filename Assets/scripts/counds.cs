using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class counds : MonoBehaviour
{
   // there are many ways to make sounds 
   //1 'put the audio in the same object
   // 2 put the audio in the object which apawn the lazer or explosion and play it when spawn the lazer   
   //3 put the sounds in the enemy and spawn it at point as audio clip cuz the enemy will be destroyed and spawn it in the camera position 
}
